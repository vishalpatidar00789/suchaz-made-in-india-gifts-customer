import styled from 'styled-components';
import { themeGet } from '@styled-system/theme-get';
const CustomizeGiftWrapper = styled.div`
    .customize-gift {
        font-size: inherit;
        color: #0066cc !important;
        padding: 0px !important;
    }
`;

export default CustomizeGiftWrapper;
